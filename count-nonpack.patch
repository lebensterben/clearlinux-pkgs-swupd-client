--- swupd-client-3.6.5/src/update.c~	2016-09-30 22:04:28.000000000 +0000
+++ swupd-client-3.6.5/src/update.c	2016-10-01 13:52:17.356965302 +0000
@@ -36,6 +36,8 @@
 #include "signature.h"
 #include "swupd.h"
 
+int nonpack;
+
 void increment_retries(int *retries, int *timeout)
 {
 	(*retries)++;
@@ -404,6 +407,8 @@
 	} else if (ret == 0) {
 		printf("Update complete. System already up-to-date at version %d\n", current_version);
 	}
+	if (nonpack)
+		printf("%i files were not in a pack\n", nonpack);
 
 	return ret;
 }
--- swupd-client-3.6.5/src/download.c~	2016-10-01 00:57:21.000000000 +0000
+++ swupd-client-3.6.5/src/download.c	2016-10-01 13:55:14.349109365 +0000
@@ -506,6 +506,8 @@
 	return 0;
 }
 
+extern int nonpack;
+
 /* full_download() attempts to enqueue a file for later asynchronous download
    - NOTE: See swupd_curl_get_file() for single file synchronous downloads. */
 void full_download(struct file *file)
@@ -525,6 +527,8 @@
 	} else if (ret < 0) { /* error */
 		goto out_bad;
 	} /* else (ret == 0)	   download needed */
+	if (!nonpack) printf("File %s was not in a pack\n", file->filename);
+	nonpack++;
 
 	curl = curl_easy_init();
 	if (curl == NULL) {
